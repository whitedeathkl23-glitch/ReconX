# ReconX
Compact, ethical website reconnaissance tool for penetration testing — headers, subdomains, ports, dirs, WHOIS, SSL and link/asset discovery

**Warning:** Only use on targets you have explicit permission to test.

## Features
- HTTP headers & status
- robots.txt & sitemap checks
- Link & asset extraction
- Small subdomain enumeration (wordlist-based)
- Basic threaded port scanner
- Directory brute-force (wordlist-based)
- WHOIS lookup
- SSL certificate info

## Requirements
Install dependencies:
```bash
pip install -r requirements.txt
